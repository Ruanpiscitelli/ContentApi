# k0s - Configuração do Serviço de Geração de Imagens
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-generator
  namespace: content-api
  labels:
    app: image-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: image-generator
  template:
    metadata:
      labels:
        app: image-generator
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8001"
    spec:
      containers:
      - name: image-generator
        image: image-generator:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        - containerPort: 8001
          name: metrics
        resources:
          limits:
            nvidia.com/gpu: 1
            memory: "16Gi"
            cpu: "4"
          requests:
            memory: "8Gi"
            cpu: "2"
        env:
        - name: API_TOKEN
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: image-gen-api-key
        - name: MINIO_ENDPOINT
          value: "minio-service:9000"
        - name: MINIO_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: minio-secrets
              key: access-key
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: minio-secrets
              key: secret-key
        - name: ETCD_ENDPOINT
          value: "http://etcd-cache-service:2379"
        - name: CACHE_PREFIX
          value: "content-api/image/"
        volumeMounts:
        - name: models-cache
          mountPath: /app/models
        - name: shared-cache
          mountPath: /app/cache
        - name: cache-config
          mountPath: /app/config/cache
          readOnly: true
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: models-cache
        persistentVolumeClaim:
          claimName: models-pvc
      - name: shared-cache
        emptyDir: {}
      - name: cache-config
        configMap:
          name: cache-config

---
apiVersion: v1
kind: Service
metadata:
  name: image-generator-service
  namespace: content-api
spec:
  selector:
    app: image-generator
  ports:
  - port: 8000
    targetPort: http
    name: http
  - port: 8001
    targetPort: metrics
    name: metrics
  type: ClusterIP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: image-generator-hpa
  namespace: content-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: image-generator
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80 