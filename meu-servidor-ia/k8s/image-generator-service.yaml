apiVersion: v1
kind: Service
metadata:
  name: image-generator-service
  labels:
    app: image-generator
spec:
  selector:
    app: image-generator
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP