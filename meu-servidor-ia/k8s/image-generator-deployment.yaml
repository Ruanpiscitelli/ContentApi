apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-generator-deployment
  labels:
    app: image-generator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: image-generator
  template:
    metadata:
      labels:
        app: image-generator
    spec:
      containers:
      - name: image-generator
        image: ruanpiscitelli/media-api:latest  # Substitua pelo endereço real da sua imagem
        ports:
        - containerPort: 8000
        env:
        - name: API_TOKEN
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: apiToken
        resources:
          limits:
            nvidia.com/gpu: "1"
          requests:
            nvidia.com/gpu: "1"
      nodeSelector:
        kubernetes.io/hostname: gpu-node  # Opcional: selecione nós com GPUs, se necessário