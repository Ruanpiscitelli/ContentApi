apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-generator-deployment
  labels:
    app: video-generator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: video-generator
  template:
    metadata:
      labels:
        app: video-generator
    spec:
      containers:
      - name: video-generator
        image: ruanpiscitelli/media-api:latest  # Substitua pelo endereço real da sua imagem
        ports:
        - containerPort: 8000
        env:
        - name: API_TOKEN
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: apiToken
        resources:
          limits:
            nvidia.com/gpu: "1"
          requests:
            nvidia.com/gpu: "1"
      nodeSelector:
        kubernetes.io/hostname: gpu-node